<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.jpetstore.dao.mybatis.mapper.TogetherMapper">
	<cache />
	
	<select id="getTogetherList" resultMap="together">
		SELECT
    		TOG_ID AS togetherId
    		TOG_NAME AS togetherName
    		HEADCOUNT AS headCount
    		TOG_DATE AS togetherDate
    		TOG_TIME AS togetherTime
    		TOG_SEX AS sex
    		TOG_AGE AS age
    		TOG_DES AS togetherDes
    		TOG_STATUS AS status
		FROM TOGETHER
	</select>
	
	<select id="getTogetherByTogId" resultMap="together">
		SELECT
    		TOG_ID AS togetherId
    		TOG_NAME AS togetherName
    		HEADCOUNT AS headCount
    		TOG_DATE AS togetherDate
    		TOG_TIME AS togetherTime
    		TOG_SEX AS sex
    		TOG_AGE AS age
    		TOG_DES AS togetherDes
    		TOG_STATUS AS status
		FROM TOGETHER
		WHERE TOG_ID = #{togId}
	</select>

	<insert id="insertTogether">
    	INSERT INTO TOGETHER (TOG_ID, TOG_NAME, HEADCOUNT, TOG_DATE, TOG_TIME, TOG_SEX, TOG_AGE,
      		TOG_DES, TOG_STATUS)
    	VALUES(#{togetherId}, #{togetherName}, #{headCount}, #{togetherDate}, #{togetherTime}, #{sex},
      		#{age}, #{togetherDes}, #{status})
	</insert>
	
</mapper>